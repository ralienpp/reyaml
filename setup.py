from distutils.core import setup
setup(
  name = 'reyaml',
  packages = ['reyaml'], # this must be the same as the name above
  version = '0.1',
  description = 'Reader of humane YAML files',
  author = 'Alex Railean',
  author_email = 'ralienpp@gmail.com',
  url = 'https://github.com/ralienpp/reyaml', # use the URL to the github repo
  download_url = 'https://github.com/ralienpp/reyaml/tarball/0.1', # I'll explain this in a second
  keywords = ['yaml', 'parser', 'configuration', 'config'], # arbitrary keywords
  classifiers = [],
)
